# puppetear

Use the puppeteer tool to navigate to [https://elfaelectric.com/](https://elfaelectric.com/).

Wait for the page to fully load so the Swiper carousel initializes.

Take a screenshot of the Hero section so you understand the layout.

Evaluate the DOM to extract the computed text colors, background colors, typography sizes, and padding for the Hero section.

Create a clean Next.js component in /src/components/sections/Hero.tsx. Do not copy Elementor's HTML structure. Write clean, semantic React code using Tailwind CSS to visually match the screenshot and computed styles you extracted. Implement the Swiper.js mechanics we discussed earlier.
